{
  "name": "cleanread",
  "version": "1.0.0",
  "description": "Chrome extension to make webpages more readable for people with dyslexia",
  "scripts": {
    "build": "tsc && npm run copy-assets",
    "copy-assets": "npm run copy-manifest && npm run copy-html && npm run copy-css && npm run copy-content && npm run copy-icons",
    "copy-manifest": "copy manifest.json dist\\",
    "copy-html": "copy popup.html dist\\ && copy options.html dist\\",
    "copy-css": "copy popup.css dist\\ && copy options.css dist\\",
    "copy-content": "xcopy src\\content\\ dist\\content\\ /E /I /y /s",
    "copy-icons": "mkdir dist\\src\\icons && copy src\\icons\\*.png dist\\src\\icons\\",
    "watch": "tsc --watch",
    "clean": "rmdir /s /q dist",
    "package": "npm run build && cd dist && powershell Compress-Archive -Path * -DestinationPath ..\\cleanread-extension.zip"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.12",
    "typescript": "^5.0.0"
  },
  "keywords": [
    "chrome-extension",
    "accessibility",
    "dyslexia",
    "readability"
  ],
  "author": "CleanRead Team",
  "license": "MIT"
}
